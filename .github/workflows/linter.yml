name: Lint Code Base

on:
  push:
  pull_request:
    branches: [master]

jobs:
  build:
    name: Lint Code Base
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read
      statuses: write

    steps:
      - uses: pre-commit-ci/lite-action@v1.0.1
        if: always()
        with:
          msg: apply pre-commit hooks